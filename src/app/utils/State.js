import {ProductDataList} from "../data/Product.data";

export const State = {
    currentActiveStar:1,
    cartCount:0,
    sortType:0,
    cartProducts:[],
    quantity:0,
    products: ProductDataList
}
